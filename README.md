Purpose:
Convert a grayscale image into a pseudocolor intensity map using a selected intensity range.
Input:
Any image file type supported by the "imread" function (default is .tif).
Specify the file in line 14.
Output:
RGB pseudocolor intensity map.
To save the output, activate lines 66–[end] in the script.
Notes:
See the header and comments in the code for details on intensity range selection and customization.
